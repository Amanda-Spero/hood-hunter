/*
const mongoose = require("mongoose");
const Schema = mongoose.Schema;

const hunterSchema = new Schema({
//Put what we want to grab//

});

const Hunter = mongoose.model("Hunter", hunterSchema);

module.exports = Hunter;


*/